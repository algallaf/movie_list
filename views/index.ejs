<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Home Page</title>
</head>
<body>
  <%- include('partials/_navbar') %>
  <div class="page-container">
    <% if (user) { %>
      <h1>Welcome to the app, <%= user.username %>!</h1>
      <p>
        <a class="add-btn" href="/users/<%= user._id %>/movies">My Movies List</a>
        <a class="add-btn" href="/auth/sign-out">Sign out</a>
      </p>
    <% } else { %>
      <h1>Welcome to the app, guest.</h1>
      <p>
        <a class="add-btn" href="/auth/sign-up">Sign up</a> or 
        <a class="add-btn" href="/auth/sign-in">Sign in</a>.
      </p>
    <% } %>
  </div>
</body>
</html>